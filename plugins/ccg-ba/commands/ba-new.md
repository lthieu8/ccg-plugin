---
description: Start a new feature spec in the vault and fill it in section by section
argument-hint: "[feature name or path to a doc in 00-inbox/]"
---

Start a new CCG feature specification.

Target: $ARGUMENTS

Use the `spec-write` skill. Load `vault-conventions` first to resolve the vault path.

If the argument names a file in `00-inbox/`, read it fully before asking anything — do not make
the BA re-explain what they already wrote down.

Interview one decision at a time, closed questions with a recommended default where one exists.
Do not generate a finished document for review.
