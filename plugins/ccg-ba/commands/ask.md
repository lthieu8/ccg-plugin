---
description: Ask what the spec says about something, answered strictly from the vault
argument-hint: "<your question>"
---

Answer this question from the CCG vault:

$ARGUMENTS

Use the `answer` skill. Load `vault-conventions` first.

Search `02-specs/`, `01-questions/`, `notes/decisions.md` and `notes/glossary.md` — all four,
including synonyms of the key terms — before concluding anything.

Answer only from what the vault says, quoting it and citing the file and section. If the vault
does not settle it, return **NOT SPECIFIED**, say what you searched, and offer to queue the
question for the BA.

Do not infer from the codebase, from how a similar feature behaves, from domain convention, or by
combining two rules into a third. A wrong confident answer costs more than no answer.
